# Python deps for the PC-side bridge.
#
# Install once per Python env (conda env or venv) after activating it:
#   pip install -r requirements.txt
#
# Then run the scripts directly:
#   python3 bridge.py
#   python3 CVMeasure.py

pyserial
pyftdi

# bridge.py exposes a small REST API alongside the TCP transport so
# browsers / other scripts can drive the motors over HTTP. fastapi
# pulls in pydantic + starlette; uvicorn is the ASGI server we launch
# from a background thread.
fastapi
uvicorn
